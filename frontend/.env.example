# Copy to frontend/.env.local (never commit secrets).
# REACT_APP_API_URL=http://localhost:8000
# Must match GOOGLE_CLIENT_ID on the API (OAuth 2.0 Web client from Google Cloud Console).
# REACT_APP_GOOGLE_CLIENT_ID=123456789-xxxx.apps.googleusercontent.com
